server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: file:///Users/gregory.eales/Developer/git/spring-petclinic-microservices-config
          default-label: springboot3

        # Use the File System Backend to avoid git pulling. Enable "native" profile in the Config Server.
        native:
          searchLocations: file:///${GIT_REPO}
